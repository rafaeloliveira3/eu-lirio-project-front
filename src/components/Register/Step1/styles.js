import styled from "styled-components";

const PasswordContainer = styled.div`
    display: flex;
    align-items: center;
    gap: 1rem;
    width: 100%;
    i {
        font-size: 1.2rem;
        width: 1.2rem;
        &:before {
            width: 1.2rem;
        }
    }
`

export {
    PasswordContainer
}